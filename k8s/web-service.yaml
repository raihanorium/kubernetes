apiVersion: v1
kind: Service
metadata:
  namespace: raihanorium
  name: web-service
  labels:
    app: web-service
spec:
  selector:
    app: web-service
  type: ClusterIP
  ports:
    - port: 8090
      targetPort: 80
